# Core libraries
numpy>=1.21.0
pandas>=1.3.0

# TensorFlow and Keras (for model training and inference)
tensorflow>=2.10.0
keras>=2.10.0

# Preprocessing and visualization (if used in ensemble.ipynb)
opencv-python>=4.5.0
Pillow>=8.0.0
matplotlib>=3.4.0
seaborn>=0.11.0

# For model ensembling (assumes use of scikit-learn metrics or logic)
scikit-learn>=1.0.0

# Streamlit for web app
streamlit>=1.15.0

# Progress bars (optional)
tqdm>=4.60.0
